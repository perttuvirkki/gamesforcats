# Copy this to either:
# - `android/app/keystore.properties` (gitignored), or
# - `android-keystore.properties` in project root (gitignored)
# and fill values.
#
# You can also set these as environment variables instead of using a file.
#
# The keystore file should typically live at `android/app/upload-keystore.jks`

UPLOAD_STORE_FILE=upload-keystore.jks
UPLOAD_STORE_PASSWORD=CHANGE_ME
UPLOAD_KEY_ALIAS=upload
UPLOAD_KEY_PASSWORD=CHANGE_ME
